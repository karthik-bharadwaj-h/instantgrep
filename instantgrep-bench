#!/bin/bash
# Run instantgrep-bench using the compiled Mix project
# Usage: ./instantgrep-bench [OPTIONS] PATH
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"
exec elixir --erl "-noinput" -S mix run -e "Instantgrep.Bench.main(System.argv())" -- "$@"
